# Domain Docs

This is a single-context repo.

Engineering skills should read these files when they exist:

- `CONTEXT.md` at the repo root for domain vocabulary and product glossary.
- `docs/adr/` for architectural decisions relevant to the work.

If these files do not exist yet, proceed silently and use the PRD plus current codebase as context.

